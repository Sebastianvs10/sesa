/**
 * Desarrollo local: ng serve (puerto 3000 por defecto en angular.json).
 * Backend por defecto: application.yml → http://localhost:8000/api
 * Autor: Ing. J Sebastian Vargas S
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api',
};
